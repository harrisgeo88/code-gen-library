import { {{ camelCase name }} } from './{{ camelCase name }}'

describe('Given a `{{ camelCase name }}` util', () => {
  describe('when it ', () => {
    it('should ', () => {
      
      // add your logic here
      expect(true).toBe(true)
    })
  })
})